# Core scientific computing
numpy>=2,<3
pandas>=2,<3
scipy>=1,<2
scikit-learn>=1,<2

# Single-cell analysis
scanpy>=1,<2
anndata>=0.12,<0.13

# scArches + HNOCA mapping (install after core packages)
scvi-tools>=0.20,<0.21
scarches>=0.6,<0.7
hnoca>=0.2,<0.3

# GP-BO
botorch>=0.17,<0.18
gpytorch>=1,<2
torch>=2,<3

# Configuration
pyyaml>=6,<7

# Testing
pytest>=9,<10
hypothesis>=6,<7

# CellRank 2 + moscot (Phase 4: temporal prediction)
cellrank
moscot

# CellFlow (Phase 5: virtual screening) - optional
# cellflow-tools

# Visualization
matplotlib>=3,<4
seaborn>=0.13,<0.14
plotly>=6,<7
